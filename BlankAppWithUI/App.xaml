<?xml version="1.0" encoding="utf-8"?>
<Application
    x:Class="BlankAppWithUI.App"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:BlankAppWithUI">
    <Application.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <XamlControlsResources xmlns="using:Microsoft.UI.Xaml.Controls" />
                <!-- Other merged dictionaries here -->
            </ResourceDictionary.MergedDictionaries>
            <!-- Other app resources here -->
            <!--<DataTemplate x:Key="AppCardTemplate">
                <Border Background="{Binding BackgroundColor}" CornerRadius="10" Margin="10" Width="150" Height="200">
                    <StackPanel VerticalAlignment="Center">
                        <Image Source="{Binding ImageSource}" Height="80" Width="80" Margin="10"/>
                        <TextBlock Text="{Binding Name}" Foreground="White" FontSize="16" HorizontalAlignment="Center"/>
                        <TextBlock Text="{Binding Rating}" Foreground="White" FontSize="14" HorizontalAlignment="Center"/>
                        <Button Content="{Binding Status}" HorizontalAlignment="Center"/>
                    </StackPanel>
                </Border>
            </DataTemplate>-->
        </ResourceDictionary>
    </Application.Resources>
</Application>
